# Configuración del analizador estático.
#
# Regla del proyecto: `flutter analyze` debe terminar SIN issues antes de cerrar
# cualquier tarea. En CI los warnings se tratan como errores (`--fatal-warnings`).
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Sube a error lo que en producción cuesta dinero o rendimiento.
    missing_required_param: error
    missing_return: error
    prefer_const_constructors: warning
    avoid_print: error
    unawaited_futures: warning
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "lib/l10n/generated/**"
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**

linter:
  rules:
    # --- Rendimiento (Módulo 3 de la guía) ---
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true
    use_colored_box: true
    use_decorated_box: true

    # --- Corrección ---
    always_declare_return_types: true
    avoid_empty_else: true
    avoid_slow_async_io: true
    cancel_subscriptions: true
    close_sinks: true
    unawaited_futures: true
    use_build_context_synchronously: true

    # --- Estilo / legibilidad ---
    prefer_single_quotes: true
    prefer_final_locals: true
    directives_ordering: true
    require_trailing_commas: true
    sort_child_properties_last: true
    unnecessary_lambdas: true
    use_super_parameters: true
